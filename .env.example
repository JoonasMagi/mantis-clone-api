# .env.example

# Secret for the session-based authentication (express-session)
SESSION_SECRET=replace_with_session_secret

# Secret for JWT authentication (if you use JWT in the future)
JWT_SECRET=replace_with_jwt_secret

# Default port on which the server runs
PORT=3000
